/* Sintaxis del operador ternario

1- (condición) ? true:false

2- (condición) ?
        (sentencia1,
        sentencia2)
        :
        (sentencia1,
        sentencia2)
*/

let num = 3;

(num % 2 == 0) ? console.log(`${num} is even`) : console.log(`${num} is odd`)